#include <stdio.h>
#include <stdlib.h>

#include "Zadanie 1c.c"
#include "Zadanie 1d.c"
#include "Zadanie 1e.c"
#include "Zadanie 2_1b.c"
#include "Zadanie 2_1c.c"

int main()
{
    /*
    const int N = 1000;
    short tab[N];
    printf("Podaj liczbe elementow: ");
    int n;
    scanf("%d", &n);

    if (n <= 0 || n > N) {
        printf("Liczba elementów musi byc z przedzialu [1, %d\n]!", N);
        return 1;
    }

    short lim = 999;
    srand(time(0));
    for (short *p = tab; p < tab + n; ++p) {
        *p = rand() % lim + 1;
    }

    for (short *p = tab; p < tab + n; ++p) {
        printf("%5d ", *p);
    }

    printf("\n");

    for (short *p = tab; p < tab + n; ++p) {
        if (*p % 2 != 0) {
            *p *= 2;
        }else {
            *p *= -1;
        }

        printf("%5d ", *p);

        if (*p > 0) {
            *p = 1;
        }else if (*p < 0) {
            *p = -1;
        }
    }
    printf("\n");
    for (short *p = tab; p < tab + n; ++p) {
        printf("%5d ", *p);
    }
    printf("\n");
    */


    //zad1c();
    //zad1d();
    //zad1e();
    //zad2_1b();
    //zad2_1c();
    return 0;
}
