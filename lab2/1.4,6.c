#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <time.h>
#include <windows.h>

/*int main()
{
    int n;
    printf("Wpisz liczbe calkowita n: ");
    scanf("%d", &n);
    int wynik;
    if (n > 2)
        {
            if(n%2==0)
            {
               for(int i = 1; i<=n; i++)
            wynik = i*n;

            printf("Iloczyn n parzystych:  %d", wynik);
            }
            else
            {
                printf("blad, liczba nieparzysta \n");
            }

        }
        if (n <= 2)
        {
            printf("liczba jest zbyt mala \n");
        }

    return 0;
}*/

