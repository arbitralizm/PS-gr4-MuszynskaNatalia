#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <time.h>
#include <windows.h>

/*int main()
{
    int n, m;
    printf("Podaj n: ");
    scanf("%d", &n);
    printf(" Podaj m: ");
    scanf("%d", &m);
    if(n<m)
        {
            for(int i = n; i<=m; i++)
            {
                int wynik;
                wynik = i*n;
                printf("Iloczyn %d \n", wynik);
            }

        }
    else
        printf("n nie jest mniejszy od m \n");
    return 0;
}*/
