#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <time.h>
#include <windows.h>

/*int main()
{
    int n, m, k;
    printf("Wpisz dodatnia liczbe n, m oraz k \n");
    scanf("%d", &n);
    scanf("%d", &m);
    scanf("%d", &k);
    printf("wszystkie wielokrotnosci n wieksze od m: ");
    for( int i = 1; n * i > m, n * i < k; i++ )
        printf("%d \n", i*n);


    return 0;
}*/
